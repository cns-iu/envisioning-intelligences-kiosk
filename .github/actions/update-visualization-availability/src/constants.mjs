/** Default repository-relative path to the exhibit data. */
export const DEFAULT_DATA_FILE = 'public/data/exhibits.yaml';

/** Default origin that embeds exhibit visualizations. */
export const DEFAULT_EMBED_ORIGIN = 'https://cns-iu.github.io';

/** Default number of retries after the initial request. */
export const DEFAULT_RETRIES = 2;

/** Default timeout for each visualization request. */
export const DEFAULT_TIMEOUT_MS = 10_000;
